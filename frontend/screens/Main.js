
import { SafeAreaView, ScrollView, Text } from "react-native";

const Main = ({ navigation, route }) => {
    <SafeAreaView>
        <Text>Main page</Text>
    </SafeAreaView>
};
export default Main;